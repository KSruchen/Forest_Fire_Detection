# Forest_Fire_Detection

This project detects the presence of forest fires from images using a Convolutional Neural Network (CNN).

The model is trained on the Wildfire Dataset and classifies images into:

• Fire
• No Fire
